{
  "name": "@civic-lens/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "corepack pnpm --filter @civic-lens/types build && corepack pnpm --filter @civic-lens/utils build",
    "dev": "next dev",
    "prebuild": "corepack pnpm --filter @civic-lens/types build && corepack pnpm --filter @civic-lens/utils build",
    "build": "next build",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "next start",
    "pretypecheck": "corepack pnpm --filter @civic-lens/types build && corepack pnpm --filter @civic-lens/utils build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@civic-lens/types": "workspace:*",
    "@civic-lens/utils": "workspace:*",
    "next": "^15.3.3",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.10",
    "@types/node": "^22.15.30",
    "@types/react": "^19.1.6",
    "@types/react-dom": "^19.1.6",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.10",
    "typescript": "^5.8.3"
  }
}
