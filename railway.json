{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && corepack pnpm install --frozen-lockfile && corepack pnpm --filter @civic-lens/api build"
  },
  "deploy": {
    "startCommand": "corepack pnpm --filter @civic-lens/api prisma:migrate:deploy && corepack pnpm --filter @civic-lens/api start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
